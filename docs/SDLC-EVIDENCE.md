# SDLC Evidence Map

**Deep phases:** 6. Release & Deployment · 7. Operations & Maintenance
**Baseline phases:** 3. Architecture & Design · 4. Implementation
**Intentionally light:** 1. Discovery & Planning, 2. Requirements Analysis,
5. Verification & Testing, 8. Retirement & Handover — because Discovery &
Planning and Verification & Testing are already demonstrated deeply in
`lexicon`, and Requirements Analysis and Retirement/Handover & Disposal are
already demonstrated deeply in `privacy-forge`. Per Rule D2 (exactly two
deep phases), this repository does not duplicate that depth — see
`docs/project-memory/00a-ledger-confirmation.md` for the full rationale.

| Phase | Depth | Evidence | Location |
|---|---|---|---|
| 1. Discovery & Planning | light | Problem framing grounded in real dogfooding (this developer's own portfolio deployments, not a hypothetical persona); build-vs-alternatives comparison (hosted SaaS / self-hosted Grafana+Prometheus / purpose-built); continuous-operation requirements (restart safety, overlapping-run prevention, retention/rollup, rolling-window SLO correctness) and the resulting plain-Postgres-vs-TimescaleDB decision; MVP boundary and non-goals table; 5 success metrics specific to continuous operation. Deliberately baseline-depth documentation (per Rule D2 — this repo's two deep phases are 6 and 7, not this one) — see `00a-ledger-confirmation.md`. | `docs/project-memory/00-project-brief.md`, `docs/project-memory/00b-build-vs-alternatives.md`, `docs/project-memory/01-scope-and-non-goals.md` |
| 2. Requirements Analysis | light | 10 user stories with Given/When/Then acceptance criteria covering every core continuous-operation flow (register HTTP/TCP check, scheduled check execution, rolling-window SLO evaluation with the pulsewatch-downtime "unknown" carve-out, threshold-breach alerting, no-alert-on-blip, both restart-safety properties, dashboard viewing, private-target monitoring via the agent); 23 functional requirements and 12 non-functional requirements with numeric targets specific to continuous operation (alert latency ≤5s, restart recovery ≤10s, 0 duplicate alerts across restart, rollup job timing, agent staleness); single-operator + machine-agent roles matrix sized to actual v1 scope (no ABAC); data classification covering target topology, alert-channel credentials, check results, and agent/operator credentials; the private-target-reachability integration requirement made concrete (agent-initiated outbound-only reporting, agent-staleness-vs-target-down distinction) rather than left as Discovery-phase justification. Deliberately baseline-depth (per Rule D2 — this repo's two deep phases are 6 and 7; Requirements is demonstrated deeply in `privacy-forge`, not duplicated here). | `docs/project-memory/02-requirements.md` |
| 3. Architecture & Design | baseline | Four real ADRs resolving requirements with no obvious default implementation: restart-safe/duplicate-proof scheduling via Postgres row leasing (ADR-0001); an explicit, database-enforced alert-suppression state machine (ADR-0002); agent-initiated push reachability and assignment discovery (ADR-0003); a bounded Go worker-pool scheduler with context-based graceful drain, directly linked to ADR-0001's leasing for shutdown safety (ADR-0004). System context and container diagrams, plus sequence diagrams for normal check execution, restart recovery, and the alert-fire-vs-suppress decision (`03-architecture.md`). Self-observability scoped concretely for OpenTelemetry (server-internal scheduler/alerting instrumentation via the same collector, no visualization backend — that would reintroduce the excluded Prometheus/Grafana third technology). Deliberately baseline-depth documentation overall (per Rule D2 — this repo's two deep phases are 6 and 7), but architecture decisions get real ADR rigor on their own merits regardless of phase depth — see `00a-ledger-confirmation.md`. **API contracts** (the piece this phase's own session explicitly deferred): a validated OpenAPI 3.1 spec (`docs/architecture/openapi.yaml`, zero warnings/errors against `@redocly/cli lint`'s `recommended` ruleset — the Node-based equivalent used in place of `openapi-spec-validator` since this environment has no working Python 3, matching `privacy-forge`'s Session 3 practice of validating the spec with a real tool) covering the operator-facing REST surface, the agent-facing assignment poll, and the OTLP/HTTP result-ingestion endpoint (ADR-0003) — with FR-023's write-once credential handling made structural (no schema field capable of returning a stored secret, for both alert-channel destinations and agent credentials), ADR-0002's three alerting states plus the ADR-0003 agent-staleness overlay surfaced precisely in the status endpoint, and idempotency defined for retried registration, duplicate agent-result submission, and credential rotation. | `docs/adr/ADR-0001-scheduler-restart-safety-leasing.md`, `docs/adr/ADR-0002-alert-suppression-state-machine.md`, `docs/adr/ADR-0003-agent-push-reachability-model.md`, `docs/adr/ADR-0004-go-concurrency-worker-pool.md`, `docs/project-memory/03-architecture.md`, `docs/project-memory/04-data-model.md`, `docs/project-memory/09-decision-log.md`, `docs/project-memory/05-api-contracts.md`, `docs/architecture/openapi.yaml` |
| 4. Implementation | baseline | **Environment, schema, and CI baseline** (Session 4 — not business-logic implementation, which starts Session 5): nine sequential `golang-migrate` migrations (`backend/migrations/`) implementing every table in `04-data-model.md`'s ERD exactly — including ADR-0001's `target_schedule` leasing columns (`lease_owner`/`lease_expires_at`), ADR-0002's `incidents` table with its `UNIQUE (target_id) WHERE closed_at IS NULL` partial-index exactly-once dispatch guard, and the `UNIQUE (target_id, checked_at)` constraint on the day-range-partitioned `check_results` table flagged by the API-contracts session for OTLP duplicate-result idempotency. Verified by actually running the migrations against a fresh Postgres 16 container (not just written): full up → down -all → up round-trip, plus functional inserts proving the partial unique index rejects a second concurrently-open incident and the ADR-0001 atomic claim statement returns zero rows on a second concurrent claim attempt. `docker-compose.yml` updated to apply these automatically (a one-shot `migrate` service gating `backend`'s startup) and to add the OTel Collector (`otel/opentelemetry-collector-contrib`, scoped to exactly the two pipelines `03-architecture.md`'s self-observability section specifies: agent-telemetry logs forwarded to the backend's `/v1/logs` endpoint with the `headers_setter` extension preserving the agent's `Authorization` bearer header end-to-end per ADR-0003, and self-observability traces/metrics exported to the `debug` exporter only — no Grafana/Prometheus backend). `backend/.golangci.yml` extended with context-propagation linters (`contextcheck`, `containedctx`, `fatcontext`, `noctx`) and `bodyclose`, justified by ADR-0004's context-cancellation-centric correctness design — one real pre-existing lint finding (`noctx` in `main_test.go`) fixed as part of turning this on. Frontend formatting baseline added (Prettier + `eslint-config-prettier`, wired into `npm run lint`/`format`). CI (`.github/workflows/ci.yml`) extended, not rebuilt (Session 0 already provides lint/vet/test/govulncheck/gitleaks/CodeQL): a Postgres service container plus an up → down -all → up migration-verification step in the backend job, and a new job validating `otel-collector-config.yaml` with the collector's own `validate` subcommand. Full `docker compose up --build` proven end-to-end on this repo: `migrate` exits `0`, Postgres/Redis/backend/frontend report `healthy`, the OTel Collector's `health_check` extension responds `200` (the collector's own images ship no shell/`wget`, so this is verified via a host-side `curl` rather than a Docker-level `healthcheck:` block — a deliberate, documented choice, not an oversight). | `backend/migrations/`, `docker-compose.yml`, `otel-collector-config.yaml`, `backend/.golangci.yml`, `frontend/.prettierrc.json`, `frontend/eslint.config.js`, `.github/workflows/ci.yml`, `CONTRIBUTING.md` |
**Session 5 — the scheduler and leasing mechanism itself**, constructed exactly to ADR-0001 (Postgres row leasing) and ADR-0004 (bounded worker pool, context-based graceful drain) with no redesign: `backend/internal/scheduler` implements the tick loop (1s default, configurable), the 20-worker (default) long-lived pool, ADR-0001's atomic conditional claim (`UPDATE target_schedule SET lease_owner=..., lease_expires_at=... WHERE next_due_at<=now() AND (lease_expires_at IS NULL OR lease_expires_at<now()) RETURNING target_id`, unchanged in predicate/columns from the ADR — only the interval literal's parameter encoding differs to fit the pgx driver), and the same-transaction release (insert `check_results`, advance `next_due_at`, clear the lease). Basic HTTP/TCP check execution per Session 2's MVP boundary (2xx = success, optional literal-substring body-match, TCP dial success/refused/timeout) — kept deliberately simple per this session's own scope, since leasing/concurrency/restart correctness was the substance. Four properties proven, not assumed, matching this portfolio's per-repo safety-critical-mechanism standard:
- **Exclusivity under genuine concurrency** (`TestClaimLease_ExactlyOneWinnerUnderRealConcurrency`, `backend/internal/scheduler/concurrency_test.go`): 20 real, independent goroutines, each racing through its own connection out of a real `pgxpool.Pool` against a real Postgres, released at a shared start barrier to claim the identical lease. Exactly one wins every run (verified `-count=3` under `-race`). **Proven to have real teeth, not just pass by construction**: the claim was temporarily weakened to a read-then-write (SELECT lease state, decide in Go, then UPDATE — no atomic WHERE guard) and the same test then failed reliably (13–14 of 20 goroutines "won" per run, across 5 repeated runs) before the atomic implementation was restored and reverified passing.
- **Restart recovery is literally the same code path, not a special case** (`TestRestartRecovery_SameTickLogicReclaimsStaleLease` and `TestClaimLease_ReclaimsLeaseAbandonedByAnotherOwner`, `backend/internal/scheduler/restart_recovery_test.go`): a lease row is seeded with a foreign, expired owner (simulating a crash mid-check), then a fresh `Scheduler.Run` — the identical method every ordinary tick uses, with no `Recover`/`RunRecovery` function existing anywhere in the package — reclaims it, executes exactly one check, and releases cleanly with `next_due_at` advanced and no duplicate `check_results` row.
- **Graceful shutdown, both outcomes** (`backend/internal/scheduler/shutdown_test.go`): `TestGracefulShutdown_InFlightCheckFinishesNaturally` proves a check already claimed when the root context cancels is not yanked — it keeps running to completion under its own per-check timeout (detached from the canceled root context by design) and records a real result. `TestGracefulShutdown_ForceCancelAtHardDeadline_LeavesLeaseAbandonedNotCorrupted` proves the other side: a check still running at the (short, test-configured) hard shutdown deadline is force-canceled, records **no** result (never a half-processed or duplicate one), leaves the lease exactly as the claim set it (abandoned, not cleared, not corrupted), and that abandoned lease is later reclaimable via the same ordinary `claimLease` predicate once it actually expires.
- Full suite (`backend/...`, unit + all four integration tests above) passes under `go test ./... -race -v -count=3`; `golangci-lint run ./...` at the pinned CI version (`v2.13.2`) is clean (`0 issues`) including the context-propagation linters Session 4 turned on — the one intentional `context.WithCancel(context.Background())` detach point (the drain context ADR-0004 requires to outlive the root context) is `//nolint:contextcheck`-annotated with its ADR rationale, not silently suppressed. `govulncheck` found and required a real fix: `golang.org/x/text` (an indirect dependency pulled in by the newly-added `github.com/jackc/pgx/v5`) was vulnerable to GO-2026-5970, reachable through `pgxpool.New`; upgraded `v0.34.0` → `v0.39.0`, reverified `0 vulnerabilities`. Also verified against the real `docker compose up --build` stack (not only throwaway test containers): a manually inserted HTTP target (`http://backend:8080/health`) and TCP target (`redis:6379`) were both claimed, checked, and recorded (`check_results.success = true`, real status code/latency) by the running `backend` container within one tick, with the lease correctly released and `next_due_at` advanced afterward. CI (`.github/workflows/ci.yml`) reordered, not rebuilt: migrations now apply once *before* `go test` (a real, necessary fix — Session 4's tests never touched Postgres, so step order didn't matter yet; Session 5's integration tests do, and would silently fail against a schema-less database without this) in addition to the existing later up→down-all→up reversibility check. Scheduler tuning (`SCHEDULER_WORKER_POOL_SIZE`/`_TICK_INTERVAL`/`_LEASE_SAFETY_MARGIN`/`_HARD_SHUTDOWN_DEADLINE`) exposed as `docker-compose.yml`/`.env.example` environment variables matching this repo's `${VAR:-default}` pattern, per ADR-0004's explicit "configuration, not hardcoded constants" consequence. | `backend/internal/scheduler/`, `backend/main.go`, `backend/go.mod`, `.github/workflows/ci.yml`, `docker-compose.yml`, `.env.example` |
**Session 6 — alert dispatch: ADR-0002's 3-state alert-suppression machine wired to Session 5's real check-execution results, with exactly-once dispatch enforced by the database, not caller discipline.** A new package, `backend/internal/alerting`, implements the machine exactly as designed, with no fourth "Unknown" state anywhere in it: `Evaluate` (`state.go`) is a pure function with no branch for Unknown at all — the scheduler simply never calls it when there is no completed check outcome to evaluate, which is the literal mechanism ADR-0002 specifies for "pauses, doesn't reset." `OpenIncident`/`CloseIncident` (`incident.go`) are ADR-0002's guarded conditional writes (`INSERT ... WHERE NOT EXISTS ... RETURNING id` / `UPDATE ... WHERE closed_at IS NULL RETURNING id`), backed by the `incidents_one_open_per_target` partial unique index Session 4 already migrated. `channel.go` implements FR-023's app-level encryption (AES-256-GCM, a fresh random nonce per encryption, key from `ALERT_CHANNEL_ENCRYPTION_KEY`) for `alert_channels.destination_encrypted`. `dispatch.go` is this session's clearly-labeled stub notification sender (`LogDispatcher`, matching this portfolio's established stub-tier pattern from bookslot/lexicon — no real webhook/email/SMS provider integration, out of scope this session) — FR-023's credential-handling contract holds even against it: the decrypted destination is real (LoadChannels genuinely decrypts it, proving the path is exercised) but is never logged, verified by a test that asserts the exact secret string never appears in the dispatcher's log output. `backend/internal/scheduler/lease.go`'s `releaseAndRecord` now performs ADR-0002's transition and guarded incidents write inside the *same* transaction as the check-result insert and lease release, exactly as the ADR specifies, reading `streak`/`state` via `SELECT ... FOR UPDATE` first; `Scheduler.handleJob` dispatches to configured channels only after that transaction actually commits and the incidents write actually returned a row (`backend/internal/scheduler/scheduler.go`). Four properties proven, not assumed, matching this portfolio's per-repo safety-critical-mechanism standard:
- **Exactly-once dispatch under real concurrency, enforced by the database, not application logic** (`TestOpenIncident_ExactlyOneWinnerUnderRealConcurrency`, `backend/internal/alerting/incident_concurrency_test.go`): 20 real, independent transactions racing to open an incident for the identical target via a shared start barrier — exactly one dispatch, every run. **Proven to have real teeth**: `TestOpenIncident_WithoutUniqueIndex_DuplicatesOccur_ThenRestored` drops the partial unique index, races ADR-0002's own explicitly-rejected "Option A" shape (a naive check-then-insert, with a deliberately widened gap — `OpenIncident`'s real single-statement `INSERT...WHERE NOT EXISTS` has too narrow a race window to reproduce the fault deterministically on its own) against the now-unguarded schema, and confirms real duplicates occur (13–14 of 20 in observed runs) — then restores the index inline and reproves the real `OpenIncident`'s exactly-once guarantee is back, with a `t.Cleanup` safety net in case of an earlier failure. This exercise surfaced and fixed a genuine bug: `OpenIncident`'s original implementation absorbed a Postgres unique-violation error without rolling back, leaving the *caller's* enclosing transaction (`releaseAndRecord`'s, in real production use) permanently aborted — silently losing that check's result along with the alert. Fixed by giving `OpenIncident` its own nested transaction via `pgx.Tx.Begin`'s built-in `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` support, so a lost race is contained to just that guarded write and never poisons an outer transaction (see `incident.go`'s own doc comment for the full mechanism). `TestCloseIncident_ExactlyOneWinnerUnderRealConcurrency` proves the same property on the resolution edge.
- **Blip suppression** (`TestEndToEnd_BlipBelowThreshold_NoIncidentNoDispatch`, `backend/internal/scheduler/alert_lifecycle_test.go`): a real target driven through the real `Scheduler.Run` tick/claim/execute/release cycle fails twice against the default `failure_threshold` of 3 (NFR-011) — `streak=2, state=suspect`, zero `incidents` rows, zero dispatch calls, proving US-006's blip zone end-to-end, not just in `Evaluate`'s own exhaustive unit tests (`TestEvaluate`, `backend/internal/alerting/state_test.go`, 8 cases covering every branch including the exact `nextStreak == threshold` edge).
- **Resolution, wired end-to-end** (`TestEndToEnd_ThresholdCrossing_DispatchesOnce_ThenResolvesOnRecovery`, same file): the real scheduler drives a target through three failures (crossing the threshold: `state=alerting`, exactly one `opened` dispatch to a real configured, decrypted channel) and then a real recovery (`state=healthy`, the incident closed, exactly one `resolved` dispatch) — proving the full pipeline (scheduler → check result → state transition → dispatch) together, not component-isolated. ADR-0002 already specifies resolution-notification behavior explicitly (dispatch exactly one resolution notification when the conditional close write returns a row) — not left ambiguous, so no new decision was required there; the real Implementation-level gaps this session did fill (AES-256-GCM as FR-023's concrete algorithm, `delivery_confirmed` reflecting only the dispatcher's own success/failure, `SELECT ... FOR UPDATE` before evaluating the transition) are recorded in `12-session-handoff.md`'s Decisions made section.
- Full suite (`backend/...`) passes under `go test ./... -race -count=3` against a real Postgres 16 container; `golangci-lint run ./...` at the pinned CI version (`v2.13.2`) is clean (`0 issues`); `govulncheck` reports `0 vulnerabilities` reachable from this repo's own code (no new dependencies were added — the new package uses only the standard library plus the existing `pgx/v5`). Also verified against the real `docker compose up --build` stack (not only throwaway test containers): two manually inserted failing HTTP targets against the running `backend` container — one with no `alert_channels` configured (crossed the threshold, opened exactly one incident, logged the graceful `ALERT_CHANNEL_ENCRYPTION_KEY not configured` warning at startup, correctly dispatched to nothing) and one with a real encrypted channel configured (crossed the threshold, dispatched exactly once via the stub, `alert_dispatches` recorded `kind=opened, delivery_confirmed=true`, and the channel's real destination string never appeared in the container's logs). `docker-compose.yml`/`.env.example` gained `ALERT_CHANNEL_ENCRYPTION_KEY` (empty by default — no channel-registration API exists yet to populate `alert_channels`, so an unset key breaks nothing; the scheduler degrades gracefully with a warning rather than failing to start). | `backend/internal/alerting/`, `backend/internal/scheduler/lease.go`, `backend/internal/scheduler/scheduler.go`, `backend/internal/scheduler/models.go`, `backend/internal/scheduler/alert_lifecycle_test.go`, `docker-compose.yml`, `.env.example` |
**Session 7 — agent communication: ADR-0003's push-only model made real (assignment discovery, OTLP result/heartbeat ingestion, distinct agent auth), with agent-reported results flowing through the identical alerting pipeline Session 6 built — no second, agent-specific evaluation path.** A new package, `backend/internal/agentauth`, is the real (not stubbed) agent-credential mechanism ADR-0003/`05-api-contracts.md` specify: `GenerateToken` mints a 256-bit `crypto/rand` bearer token; `HashToken` (plain SHA-256, an implementation-level algorithm choice this session fills in — a slow KDF earns nothing defending a secret that's already 256 bits of uniform randomness, unlike Session 6's AES-256-GCM choice for a *symmetric* secret) is what `agents.credential_hash` stores; `Authenticate` looks up by hash and returns the caller's own `Identity`, never another agent's; `IsStale`/`FetchLiveness`/`RecordHeartbeat` implement NFR-008's `now() - last_heartbeat_at > 3 × report_interval_seconds` rule and the monotonic (`GREATEST`) heartbeat write ADR-0003 specifies. A new package, `backend/internal/agentapi`, implements the two agent-facing endpoints exactly to `openapi.yaml`: `GetAssignments` (`GET /api/v1/agent/assignments`, scoped only by the caller's bearer identity — no `{agent_id}` path parameter exists to even construct a cross-agent request, and deliberately never touches `last_heartbeat_at`) and `IngestLogs` (`POST /v1/logs`, the simplified OTLP/HTTP+JSON shape, both `check_result` and `heartbeat` record types, per-record partial-success rejection for a malformed record or a target the caller isn't assigned to). `backend/internal/alerting` gained `RecordCheckResult` (`recordresult.go`) — the single write path (idempotent `INSERT ... ON CONFLICT (target_id, checked_at) DO NOTHING`, `SELECT ... FOR UPDATE`, `Evaluate`, `ApplyIncidentAction`) that **both** `scheduler.releaseAndRecord` (refactored, not reimplemented — same SQL, same transaction boundaries, verified via A/B testing below to introduce no behavioral change) and `agentapi`'s new `recordAgentCheckResult` now call, and `DisplayState` (`display.go`) — the pure ADR-0002-Consequences/ADR-0003-Decision-3 read-time overlay (`unknown` iff agent-assigned and stale, else `raw_state` unchanged, `raw_state` itself never touched). A real, minimal reference agent binary (`backend/cmd/agent`) runs its own local due-set scheduler (no Postgres access, per `03-architecture.md`), executes real HTTP/TCP checks, and reports via real outbound HTTP POSTs — used as the actual agent in this session's end-to-end proof, not simulated by curl. **Agent provisioning is real but deliberately not exposed as an unauthenticated HTTP endpoint**: `docs/architecture/openapi.yaml`'s `POST /api/v1/agents` requires `operatorSession` auth, and no operator-session mechanism (the stateless HMAC-signed cookie `05-api-contracts.md` designs) exists anywhere in this repo yet — the same class of gap Sessions 5/6 already flagged for target/alert-channel registration. Shipping that endpoint open would be a real regression in a public repo, not a reasonable interim step, so this session exposes the identical real mechanism (`agentauth.CreateAgent`) via a local admin CLI, `backend/cmd/seed-agent`, instead — flagged explicitly in `12-session-handoff.md`, not silently worked around.

- **Agent auth, real provisioning and verification** (`backend/internal/agentauth/*_test.go`): `TestAuthenticate_RealCredentialRoundTrip`/`_UnknownTokenRejected`/`_EmptyTokenRejected`/`_DistinctAgentsDistinctCredentials` prove the real token → hash → lookup round trip against a real Postgres `agents` row (via `CreateAgent`, never a hand-rolled fixture with a fake hash), that an unissued or empty token is rejected, and that two agents' credentials never cross-authenticate. `TestCreateAgent_StoresOnlyTheHashNeverThePlaintext` guards against the regression that would silently defeat hashing entirely. `TestIsStale` is table-driven over the exact `> 3×` boundary (not `>=`) and the never-heartbeated case; `TestRecordHeartbeat_RealMonotonicUpdate` proves the real `GREATEST`-based write never regresses on an out-of-order report, against a real Postgres row.
- **OTLP duplicate-result idempotency, proven at both the shared write path and the full HTTP endpoint** (`docs/architecture/openapi.yaml`'s explicit requirement — "the dedup check must gate entry to the transition function, not just the final write"): `TestRecordCheckResult_DuplicateCheckedAtIsANoOp` (`backend/internal/alerting/recordresult_test.go`) resubmits an identical `(target_id, checked_at)` pair directly against `RecordCheckResult` and proves streak stays at 1 (not double-incremented to 2), no second `check_results` row, no re-entry into `Evaluate`. `TestIngestLogs_DuplicateCheckResult_IsANoOp` (`backend/internal/agentapi/logs_test.go`) proves the identical property one layer up, through two real HTTP `POST /v1/logs` requests against the real router.
- **The staleness-vs-target-down evaluation-ordering rule, proven with both halves explicit** (`TestStalenessVsTargetDown_BothHalvesDistinguished`, `backend/internal/agentapi/logs_test.go`): a real agent reports a real heartbeat, then three real `check_result` failures over real HTTP — `raw_state` genuinely reaches `alerting` (a real incident opens, exactly one real dispatch fires, proving "a genuinely-down target reported by a healthy agent transitions through the alert machine normally" — half one). The *identical* `target_schedule` row, read back once `agentauth.IsStale` computes the agent as stale (no further heartbeat, `> 3×report_interval` elapsed), is shown by `alerting.DisplayState` as `"unknown"` — even though `raw_state` in the database is still, unmodified, exactly `alerting` (asserted directly against Postgres after the display-time computation) — proving half two: a stale agent's real last-known result is never read as still current, and staleness is surfaced as its own distinct condition rather than silently reinterpreted as "all its targets are down."
- **Cross-agent write rejection** (`TestIngestLogs_CrossAgentTargetRejected`, `backend/internal/agentapi/logs_test.go`): an agent presenting a valid credential for a target it is not assigned to is rejected via partial success (not written), the write-side symmetry of `05-api-contracts.md`'s "no `{agent_id}` in `/agent/assignments`" structural guarantee.
- **A real end-to-end proof through the actual `docker compose up --build` stack, driven by the real reference agent binary** (not simulated by curl): `backend/cmd/seed-agent` provisioned a real agent (`agentauth.CreateAgent` against the running stack's own Postgres) and printed its one-time bearer token; a real target was assigned to it pointing at a guaranteed-404 path on the running `backend` container (`failure_threshold=2`, `interval_seconds=10`); a real AES-256-GCM-encrypted `alert_channels` row was inserted with a freshly generated key supplied to `backend` via `ALERT_CHANNEL_ENCRYPTION_KEY`. `backend/cmd/agent`, run as its own container on the compose network with only its bearer token/agent id/server URL as configuration (no Postgres access, no shared code beyond the wire-shape types in `agentapi`), polled `GET /api/v1/agent/assignments`, executed ten real HTTP checks against the real 404 endpoint, and `POST`ed each real `check_result` plus periodic real `heartbeat` records to `/v1/logs` — all over genuine outbound HTTP, no inbound port opened (FR-018). Verified directly against Postgres afterward: `agents.last_heartbeat_at` freshly updated by the real heartbeats; ten real `check_results` rows; `target_schedule` at `streak=10, state=alerting`; **exactly one** `incidents` row, opened on the second failure (matching `failure_threshold=2`, never reopened across the remaining eight); **exactly one** `alert_dispatches` row (`kind=opened, delivery_confirmed=true`) to the real channel. All of this session's own test data (the agent, target, schedule, results, incident, dispatch, channel rows) was deleted afterward; `privacy-forge`, `laravel-consent-guard`, `bookslot`, and `lexicon` were not touched.
- Full suite (`backend/...`) passes clean under `go test ./... -race -v` (the exact command `.github/workflows/ci.yml` runs); `golangci-lint run ./...` at the pinned CI version (`v2.13.2`) is clean (`0 issues` — 13 real findings from this session's own new code, all fixed: unchecked `Fprintf`/`Close` errors, `net.Listen` without a context, two missing exported-symbol doc comments, one malformed comment, one self-comparison test bug); `govulncheck` reports `0 vulnerabilities` reachable from this repo's own code (no new external dependency — `agentauth` uses only `crypto/rand`/`crypto/sha256`/`crypto/subtle`/stdlib plus the already-present `pgx/v5`). **A real, measured infrastructure finding, not a code defect**: under this session's own heavier `-race -count=3` verification standard, `docker stats` showed this developer's shared Docker Desktop host (5.7GB total memory) concurrently running several unrelated, already-started containers from other projects on this machine (one measured at 60% CPU) while `go test ./...`'s default cross-package parallelism opened many simultaneous connections to one small `postgres:16-alpine` test container; a direct A/B rerun of the pre-existing `alert_lifecycle_test.go` end-to-end tests against the *unmodified* Session 6 code reproduced the identical intermittent `waitForCondition` timeout under that same contention, proving it is a pre-existing polling-margin sensitivity exposed by this shared/loaded host, not a Session 7 regression (`backend/internal/scheduler/lease.go`'s refactor is a pure delegation to the shared `alerting.RecordCheckResult` — same statements, same transaction shape). `go test -p 1` (serializing package execution, removing the cross-package contention variable entirely) passes `-race -count=3` clean and repeatably; the polling budget in `alert_lifecycle_test.go` was also widened (2s → 15s, documented inline with this finding) as a real, low-risk test-robustness fix below ADR-0002's own level of specification — GitHub Actions' actual dedicated CI runner carries none of this shared-host contention. | `backend/internal/agentauth/`, `backend/internal/agentapi/`, `backend/internal/alerting/recordresult.go`, `backend/internal/alerting/display.go`, `backend/internal/scheduler/lease.go`, `backend/cmd/agent/`, `backend/cmd/seed-agent/`, `backend/main.go` |
**Session 8 — operator authentication and the operator-facing REST API: closing the gap Sessions 5–7 each flagged in turn, made real rather than assumed.** A real audit before writing code (`12-session-handoff.md`) found the task's own framing understated the gap: not just `POST /api/v1/agents`, but *every* operator-facing HTTP handler was missing — `backend/main.go`'s router wired only `/health` and the agent-facing surface; `05-api-contracts.md`'s entire mutating/registration surface (targets, alert channels, agents) existed only as underlying functions and test-only direct-SQL fixtures, never a reachable endpoint. Two new packages close this exactly to the already-designed contract, not a redesign: `backend/internal/operatorauth` implements `05-api-contracts.md`'s stateless HMAC-SHA256-signed session cookie (`IssueSession`/`VerifySession`, `SessionCookieName`/`SessionTTL`, `SigningSecretFromEnv` requiring `SESSION_SIGNING_SECRET` with no fallback) and real password handling (`HashPassword`/`VerifyPassword` via bcrypt — `golang.org/x/crypto/bcrypt`, an implementation-level algorithm choice for a human-chosen secret, mirroring how Session 7 chose SHA-256 for a differently-shaped one — plus `Login`, one `ErrInvalidCredential` for both "unknown email" and "wrong password" with a fixed dummy-hash comparison closing the timing side channel too). `backend/internal/operatorapi` implements the full gated surface — `RequireOperator` (verifies and discards identity; `02-requirements.md`'s single-operator baseline means no handler needs to know *which* operator, unlike `agentapi.RequireAgent`'s legitimate multi-agent case), an in-process fixed-window login rate limiter (5/15min, keyed by email and IP, no Redis dependency — the same "not load-bearing" reasoning `03-architecture.md` already applies to Redis for scheduling), `RequireJSONContentType` as this session's specifically-reasoned CSRF mitigation (`SameSite=Strict` + no CORS + JSON-only bodies — re-derived for this app's SPA-with-strict-cookie shape, not `privacy-forge`'s server-rendered-form token mechanism copied unexamined), and full CRUD for targets/alert-channels/agents. Building `CreateTarget` surfaced and fixed a real, previously-latent bug: no code path anywhere in the repo had ever inserted the `target_schedule` row a newly registered target needs to be scheduled at all (`scheduler.go`'s due-set query depends on it) — every prior session's own fixtures inserted it by hand because the real handler didn't exist yet; it's now inserted in the same transaction as the target itself. `backend/internal/agentauth` gained `RotateCredential` (needed for the new `POST /agents/{id}/credential/rotate`, absent since Session 7's scope was creation only). A real bootstrapping CLI, `backend/cmd/provision-operator`, solves `02-requirements.md`'s single-operator chicken-and-egg problem the same honest way `bookslot` solves its own first-owner-account gap — reads the password from stdin (never a `-password` flag, avoiding shell-history/`ps` exposure), refuses to run if any operator already exists. `backend/cmd/seed-agent` is kept, not retired: its package doc comment was rewritten to state the real, examined distinction — it needs only `DATABASE_URL`, never the HTTP server or a session, so it remains a genuine break-glass path when the API process itself is unreachable, while `POST /api/v1/agents` (proven to produce an interchangeable credential, since both call the identical `agentauth.CreateAgent`) is now the correct path for ordinary use.
- **Unauthenticated-rejection audit, exhaustive and table-driven, not assumed from middleware existing** (`TestEveryOperatorFacingEndpoint_RejectsUnauthenticatedRequests`, `backend/internal/operatorapi/middleware_test.go`): every route this session's router wires (`/auth/logout`, all target/alert-channel/agent endpoints) is issued a real request with no session cookie and asserted `401` individually; `TestEveryOperatorFacingEndpoint_RejectsInvalidSessionCookie` and `TestEveryOperatorFacingEndpoint_RejectsAnAgentToken` prove the same for a bogus cookie and for a real `agentToken` bearer value presented as a session cookie (the credential-shape-disjointness property `05-api-contracts.md` documents — `401`, never a `403` that doesn't exist in this system).
- **Real login → real gated request, proven twice — once against the real router with real Postgres, once against the actual compiled binary**: `TestLogin_RealCredentialIssuesAWorkingSession` (`operatorapi/auth_test.go`) decodes the real `Set-Cookie` header (`HttpOnly`/`Secure`/`SameSite=Strict` asserted directly, not assumed from the code that sets them) and replays that exact cookie against a real gated request. Separately, `backend/Dockerfile`'s actual image was built and run as its own container against an isolated `postgres:16-alpine` container on a dedicated Docker network (no host port binding required); `cmd/provision-operator` created a real first operator against it; `curl` proved, against the running binary: unauthenticated `GET /targets` → `401`; wrong password → `401`; correct login → `200` with a real `Set-Cookie`; that cookie replayed on `POST /targets` and `POST /agents` → real `201`s; the agent's plaintext token replayed as a bearer credential against the real `agentapi` router's `GET /agent/assignments` → `200` — the direct proof that an operator-provisioned agent credential is genuinely interchangeable with a `cmd/seed-agent`-provisioned one. This isolated verification environment (container, network, image, two Go cache volumes) was fully torn down afterward; the real `docker-compose.yml` stack's own persistent volume was never started or touched.
- **Credential rotation and agent decommissioning's `409` rule, proven end to end through real HTTP**: `TestRotateAgentCredential_ThroughRealGatedHTTP` (`operatorapi/agents_test.go`) and `TestRotateCredential_OldTokenStopsWorkingNewTokenWorks` (`agentauth/rotate_test.go`) prove the pre-rotation token is rejected by the real `agentapi` router immediately after rotation while the new one authenticates; `TestDeleteAgent_BlockedWhileTargetsAssigned` proves `409` while a target is assigned, `PATCH`es the target's `agent_id` to `null` (exercising the raw-key-presence null-vs-absent handling `UpdateTarget` needs since a plain nil-pointer check can't distinguish "clear this field" from "field not mentioned"), then proves `204` once unassigned.
- **FR-023's write-once-secret property, checked against the raw wire body, not just the Go struct** (`operatorapi/alertchannels_test.go`): a real destination string is asserted absent from the `POST`/`GET`/list response bodies and from `alert_channels.destination_encrypted` itself via direct substring search, extending the identical discipline `agentapi`'s own agent-credential tests already hold to this system's second secret.
- **The previously-latent `target_schedule` gap, proven fixed, not merely described**: `TestTargets_FullCrudLifecycle_ThroughRealGatedHTTP` asserts exactly one `target_schedule` row exists immediately after a real `POST /targets` call.
- **The bootstrapping CLI's single-operator guard, both directions** (`cmd/provision-operator/main_test.go`): `TestRun_RealProvisioning` proves a genuine `operators` row with a genuine bcrypt hash (against a real Postgres, skipping rather than false-failing if the shared test database isn't observably empty at that instant, since `go test ./...`'s cross-package parallelism means other packages' own operator fixtures can transiently exist); `TestRun_RefusesWhenAnOperatorAlreadyExists` seeds one operator directly and proves a second CLI invocation refuses and creates nothing.
- **Login rate limiting has real teeth** (`TestLogin_RateLimitedAfterRepeatedFailures`): repeated wrong-password attempts against the real router eventually produce `429`, not an unbounded run of `401`s; **the account-enumeration/timing side channel is closed** (`TestLogin_UnknownEmailAndWrongPasswordReturnTheIdenticalError`): both failure modes return the identical error value from a comparably-costly bcrypt comparison.
- Full suite passes: `go test ./... -race -v` — **111 tests, 0 failures, 0 skips**, all ten packages, run via an ephemeral `golang:1.25` container against a real, isolated `postgres:16-alpine` container (this environment has no native Go toolchain, matching every prior session). `go vet ./...`: clean. `golangci-lint run ./...` (pinned `v2.13.2`): clean, `0 issues` — one real finding from this session's own new code (an unused `identityFromContext` helper in `operatorapi/middleware.go`, removed along with the context value it read, since `RequireOperator` verifying-and-discarding the identity is this package's correct shape, not a half-finished feature). `govulncheck`: `0` vulnerabilities reachable from this repo's own code — `bcrypt` added no new external module, only promoted an already-present indirect dependency (`golang.org/x/crypto`, already pulled in via `gin`'s own dependency graph) to direct in `go.mod`. Confirmed `privacy-forge`, `laravel-consent-guard`, `bookslot`, and `lexicon` were not touched. | `backend/internal/operatorauth/`, `backend/internal/operatorapi/`, `backend/internal/agentauth/rotate.go`, `backend/cmd/provision-operator/`, `backend/cmd/seed-agent/main.go`, `backend/main.go`, `backend/go.mod`, `docker-compose.yml`, `.env.example` |
**Session 9 — the first dashboard-read endpoint and the first real SvelteKit screen: `GET /api/v1/targets/{target_id}/status` exactly to `openapi.yaml`'s already-specified `TargetStatus` schema, and a login-gated dashboard consuming it, with no new status computation and no mutation of `raw_state`.** `backend/internal/operatorapi/status.go` is a pure read composed entirely from functions Sessions 6/7 already wrote and tested — `alerting.DisplayState` (the ADR-0002-Consequences staleness overlay) and `agentauth.IsStale`/`FetchLiveness` (ADR-0003/NFR-008) — joining `targets`/`target_schedule` for the base row and reading `incidents` directly when `raw_state = alerting`, matching `05-api-contracts.md`'s "the overlay is computed at request time, never a maintained flag" property exactly; no ADR reopened, no new column, no write path added to this handler at all. `frontend/src/routes/login/` and `frontend/src/routes/dashboard/` are this repo's first application code beyond the Session 0 skeleton: a `+page.server.ts` form action calls the real `POST /api/v1/auth/login` server-side (`frontend/src/lib/server/backend.ts`), and a `+page.server.ts` load function calls the real `GET /targets` + the new `GET /targets/{id}/status` per target (unpaginated, matching `05-api-contracts.md`'s own bounded-scale reasoning rather than inventing a bulk endpoint the spec doesn't define) — a 401 from the backend, not merely a missing cookie, is what actually redirects to `/login`, so gating is decided by Session 8's real auth every request, never assumed from cookie presence alone. **A real, previously-latent gap this session's own frontend work surfaced and fixed, not related to the dashboard itself**: no `.svelte` file in this repo had ever used `lang="ts"` before this session, so `frontend/eslint.config.js`'s `svelte.configs['flat/recommended']` had never been exercised against one — it silently mis-parsed `import type`/`$props()` syntax as plain JS. Fixed by adding the documented `parserOptions.parser: tseslint.parser` mapping for `**/*.svelte` files; `npm run lint` is clean project-wide, not merely on this session's own new files.
- **A real, structural finding named rather than routed around**: `05-api-contracts.md`'s `operatorSession` cookie is `Secure` (HTTPS-only) by design, but no session before this one had given the project any TLS termination story — `docker-compose.yml` serves both `backend` and `frontend` over plain HTTP, and `08-deployment-and-operations.md`/`06-security-threat-model.md` are silent on it. A real browser refuses to store a `Secure` cookie set over `http://`, so this session's own verification standard ("prove it in a real browser") could not be met against the stack as it stands — flagged to the user directly rather than quietly flipping `Secure` off to make local verification easier (the exact "temporarily weaken auth" shortcut this session's ground rules warned against). Per the user's explicit choice, verification used locally-generated self-signed certificates and a `caddy:2-alpine` TLS terminator in front of the `vite dev` process, for this session's own proof only — no application code, `docker-compose.yml`, or `.env.example` changed as a result, and the verification containers/certs were torn down afterward. The real production TLS/reverse-proxy story remains a named, unresolved gap for a future deployment-focused session (see `12-session-handoff.md`).
- **The new endpoint, proven against real Postgres**, `backend/internal/operatorapi/status_test.go`: `TestGetTargetStatus_ServerExecutedHealthyTarget` (a fresh target reads `healthy`/`healthy`, nil `agent_id`/`agent_stale`, no `open_incident`); `TestGetTargetStatus_UnknownIdReturnsNotFound`; `TestGetTargetStatus_OpenIncidentSurfaced` (a real `incidents` row with `raw_state='alerting'` is reflected in `open_incident`); and this session's own "prove it has teeth" case, `TestGetTargetStatus_StaleAgentDisplaysUnknownWithoutMutatingRawState` — a target assigned to a real agent whose `last_heartbeat_at` is set an hour in the past (report interval 60s, so past the `3×` staleness threshold) reads `display_state="unknown"` while `raw_state`/`streak`/`last_checked_at` are asserted byte-for-byte identical in `target_schedule` before and after the HTTP request. `TestEveryOperatorFacingEndpoint_RejectsUnauthenticatedRequests` (`middleware_test.go`) gained the new route, keeping that audit table exhaustive against `router.go`.
- **The identical no-mutation property, reproven live against the actual compiled binary, not only Go's own test harness**: a real target was assigned to a real agent via `POST /api/v1/agents` + `PATCH /targets/{id}`, `target_schedule` set to `state='suspect', streak=2` and the agent's `last_heartbeat_at` pushed an hour into the past via direct SQL (simulating real elapsed staleness, not a fabricated fixture shape) — `SELECT state, streak, last_checked_at FROM target_schedule` read identical before and after `curl GET /targets/{id}/status`, while the response itself showed `display_state="unknown"`, `raw_state="suspect"` (verbatim), `agent_stale=true`. A second real target (server-executed, real invalid URL) was left running against the actual scheduler inside the compiled binary and organically crossed its failure threshold into a real `alerting` state with a real open incident during verification — proving the dashboard renders genuinely live, scheduler-computed state, not a canned fixture. Unauthenticated `GET /targets/{id}/status` → real `401`, both directly against the backend and through the frontend's own gated `/dashboard` route (→ real `303` to `/login`).
- **The dashboard screen itself, proven end-to-end through a real browser-equivalent HTTPS flow** (curl driving a cookie jar through a real `caddy:2-alpine` TLS terminator in front of the actual `vite dev` process — the verification method the user chose for the Secure-cookie gap above): `POST /login` → a real `Set-Cookie: pulsewatch_session=...; HttpOnly; Secure; SameSite=Strict` issued by the frontend's own origin (not forwarded verbatim from the backend's own cookie — parsed and re-issued, since the backend's cookie targets the backend's own origin, which the browser in this design never talks to directly); that cookie replayed on `GET /dashboard` renders the real target rows with correct status badges; `POST /dashboard?/logout` clears it (`Max-Age=0`) and a subsequent `/dashboard` request redirects to `/login` again. `npm run test`/`npm run check`/`npm run lint` all pass (the one remaining `svelte-check` finding, `vite.config.ts`'s vitest `test` key, predates this session and is unrelated to any file this session touched).
- **A real, environmental (not code) finding surfaced by this session's own full-suite verification, reported rather than silently patched around**: `go test ./... -race -v` (113 passed, 2 failed) showed `internal/alerting`'s `TestRecordCheckResult_DuplicateCheckedAtIsANoOp` and `TestRecordCheckResult_ThresholdCrossingOpensIncidentDispatch` fail under the full cross-package concurrent load this verification host produces against one small shared `postgres:16-alpine` container — the same class of shared-host contention Session 7's own evidence entry above already documented and reasoned about for a different test file. Re-run in isolation (`-run TestRecordCheckResult -count=3`, three repetitions) both tests passed cleanly nine times running; this session made no change to `internal/alerting` (out of this session's scope — ADR-0002 and that package are untouched). Flagged for Session 10 in `12-session-handoff.md` as a real, reproducible-under-load flake to watch for, not fixed here. | `backend/internal/operatorapi/status.go`, `backend/internal/operatorapi/status_test.go`, `backend/internal/operatorapi/router.go`, `backend/internal/operatorapi/middleware_test.go`, `frontend/src/lib/server/backend.ts`, `frontend/src/routes/login/`, `frontend/src/routes/dashboard/`, `frontend/src/routes/+page.svelte`, `frontend/eslint.config.js` |
| 5. Verification & Testing | light | | |
| 6. Release & Deployment | **deep** | | |
| 7. Operations & Maintenance | **deep** | | |
| 8. Retirement & Handover | light | | |

## Why some phases are light

To be filled in as each phase's session lands, per
`docs/project-memory/00a-ledger-confirmation.md`.
