# Backlog
> Purpose: everything deliberately not being done right now
> Project: pulsewatch (public)
> Last updated: 2026-09-06 (Session 16)

## Next up
| ID | Item | Type | Size | Why now |
|---|---|---|---|---|
| B-009 | **Substantially closed, Session 15 — see ADR-0005's Session 15 update and `10-risk-register.md`'s R-008.** The Kubernetes workload half (`deploy/k8s/base/`'s manifests, applied for real against a real self-built single-node cluster, including a real migration run and real rolling updates) is now real-cluster-verified. Remaining, narrower scope tracked as B-013 below: `terraform apply` specifically (blocked by explicit `registry.terraform.io` policy, not a capability gap), cert-manager/Let's Encrypt issuance, NetworkPolicy enforcement on a real policy-capable CNI, and frontend/otel-collector images. | Infra / Verification | Was Medium, now Small (narrowed) | ADR-0005 (Session 14) authored the Terraform module and Kubernetes manifests but this sandbox had no registry egress or live cluster/Docker daemon to apply against (R-008, `10-risk-register.md`) — this project's own standard is real evidence, not "looks right," and Session 15 closed the part of that gap a sandbox without registry access could ever close |
| B-013 | Finish B-009's remaining scope once a sandbox with the right egress allows it: real `terraform apply` (needs `registry.terraform.io` access specifically), cert-manager/Let's Encrypt real certificate issuance, NetworkPolicy enforcement verified against a real policy-capable CNI (Calico/Cilium/cloud-provider), and frontend + otel-collector images built and deployed (same from-source approach Session 15 used for postgres/redis/backend/agent, not attempted for these two for time) | Infra / Verification | Medium | Session 15 (B-009) proved the Kubernetes workload layer for real but named these four items as out of this sandbox's reach specifically — not because they're harder to verify in principle, only because this sandbox's egress policy blocks `registry.terraform.io` and all container-registry blob downloads; a session with either capability should pick this up rather than re-deriving Session 15's own from-scratch cluster-building work |
| B-010 | A cloud-VM-plus-k3s Terraform bootstrap module, for an operator with no existing Kubernetes cluster | Infra | Medium-Large | Deliberately out of ADR-0005's own scope (Decision: BYO-cluster only) — a real cloud provider means real per-provider credentials and cost, a materially different trade-off needing its own options-considered pass, not an extension of the BYO-cluster decision |
| B-012 | Fill `06-security-threat-model.md` and `07-testing-strategy.md` for the whole system — both were discovered, Session 14, to be empty section-header skeletons beyond the small IaC/K8s-scoped content that session added | Docs | Medium-Large | A real, pre-existing gap independent of Session 14's own IaC/K8s work (not introduced by it) — surfaced while adding that session's own secrets-management/CI-gate notes to each file and finding almost nothing else there to integrate with |
| B-011 | Postgres backup/restore in the Kubernetes target (a scheduled `CronJob` running `pg_dump` to persistent storage, plus a documented restore drill) | Ops | Medium | `08-deployment-and-operations.md`'s existing "Backup and restore" section says "not built this session" for Compose too — Kubernetes gaining a real production target (ADR-0005) is what makes this gap newly load-bearing rather than a documented absence on a dev-only deployment |
| B-007 | Operator password reset/rotation path (a real endpoint or CLI, not raw `psql`) | Feature / Ops | Small-Medium | `provision-operator`'s own doc comment already named this gap; Session 13 had to exercise the only documented break-glass path (a throwaway Go program computing a bcrypt hash and writing it directly via `psql`) to get a real login for `05-api-contracts.md`'s own verification standard. A single-operator project with no recovery path if that operator forgets their password is a real operational gap, not just a nice-to-have. |
| B-004 | SLO-threshold alerting (e.g. "alert if uptime drops below X%") | Feature | Medium | Explicitly named out of scope for Session 12 — a new alert-machine capability, belongs in its own session with its own ADR-adjacent design, not folded into the read-only `/slo` endpoint |
| B-005 | Configurable/custom rolling windows per target | Feature | Small-Medium | Explicitly named out of scope for Session 12, which kept `/slo`'s existing request-time `window_days`/`slo_target_pct` parameters (already in `openapi.yaml`) but added no *stored per-target* window configuration — a real future feature if an operator ever wants a target's dashboard uptime figure to default to something other than the shared 30-day/99.9% default |
| B-006 | Fix test-fixture cleanup ordering across `scheduler`/`agentapi`/`alerting`/`operatorapi` (pre-Session-12 tests) so `DELETE FROM targets` in `t.Cleanup` doesn't silently fail when `check_results`/`incidents` rows still reference the fixture | Testing / hygiene | Small | R-005 (`10-risk-register.md`, opened Session 12): confirmed to leave real orphaned rows in a developer's persistent local Postgres on every full `go test ./...` run; Session 12 fixed its own two new call sites but left the broader, pre-existing gap alone as out of scope |
| B-008 | Server-side session revocation on `POST /auth/logout` (today it only clears the browser's cookie via `Set-Cookie: Max-Age=0`; the token itself, if replayed directly, stays valid until its natural expiry) | Security / Ops | Small-Medium | Found incidentally during the 2026-09-01 evidence re-verification pass while trying to invalidate a captured session cookie before committing evidence to this public repo: `POST /auth/logout` returned `204`, but the same token replayed against `GET /api/v1/targets` still returned `200`. No `Revoke`/`Invalidate` function exists in `internal/operatorauth`. Low severity today (no real remote deployment exists yet — R-003's gate; a captured token only matters to someone who already has access to this dev machine), but worth closing before R-003 is closed. |

## Later

## Explicitly rejected (with reasons)
| ID | Item | Reason |
|---|---|---|
| — | Retention job's daily rollup tier | `04-data-model.md`'s own reasoning: 400 days × 24 buckets/target is trivially small for Postgres to scan directly; a second tier would be premature machinery. Revisit only against measured evidence, not by default. |

## Done (kept for traceability, not re-litigated)
| ID | Item | Closed |
|---|---|---|
| B-001 | `GET /targets/{id}/slo` + FR-008's hourly rollup job | Session 12 — see `04-data-model.md`'s Session 12 addendum and `12-session-handoff.md` |
| B-003 | Real TLS/reverse-proxy story for `docker-compose.yml` | Session 10 (R-001) |
| B-002 | `GET /targets/{id}/incidents` | Session 16 — a pure read of the `incidents` table ADR-0002's already-implemented `OpenIncident`/`CloseIncident` (`internal/alerting`) already writes; no new incident-detection logic was needed or added, matching this item's own "no new computation" framing |
